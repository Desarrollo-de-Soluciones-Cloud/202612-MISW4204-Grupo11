package domain

// Nombres de rol globales (coinciden con la tabla roles tras la migración).
const (
	RolAdministrador     = "administrador"
	RolProfesor          = "profesor"
	RolMonitor           = "monitor"
	RolAsistenteGraduado = "asistente_graduado"
)
